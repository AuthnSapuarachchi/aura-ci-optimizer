# Aura Frontend (React)

This is the React UI for the Aura project.

## How to Run Locally
1.  `cd` into the `frontend` folder.
2.  Run `npm install` (only needed the first time).
3.  Run `npm start`.
4.  The app will be live at `http://localhost:3000`.
